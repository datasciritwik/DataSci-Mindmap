# === Core App ===
streamlit>=1.50.0
rich>=14.0.0

# === Optional: legacy Google TTS ===
gTTS>=2.5.4

# === MeloTTS (direct from GitHub to avoid PyPI build issues) ===
git+https://github.com/myshell-ai/MeloTTS.git

# === Core dependencies for MeloTTS ===
torch>=2.2.0
torchaudio>=2.2.0
numpy>=1.26.0
soundfile>=0.12.1
scipy>=1.11.0

# === Audio processing (for conversion, optional) ===
pydub>=0.25.1
ffmpeg-python>=0.2.0

# === Fix for Streamlit Cloud build (skip Japanese tokenizer deps) ===
fugashi==1.3.0 --prefer-binary
unidic-lite>=1.0.8
